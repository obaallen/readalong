{
  "name": "readalong",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter web dev",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
